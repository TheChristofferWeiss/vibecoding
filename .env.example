# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api

NEXT_PUBLIC_SUPABASE_URL=your-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Optional: Service role key (server-side only, never expose to client!)
# Required for admin operations like user invitations and deletions
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional: Production URL (for email links and redirects)
# Vercel automatically sets NEXT_PUBLIC_VERCEL_URL, so this is optional
NEXT_PUBLIC_SITE_URL=http://localhost:3000

